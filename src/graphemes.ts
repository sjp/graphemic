// Operations measured in graphemes. Also the `@sjpnz/graphemic/graphemes`
// entry point.

// Empty for now, but a module rather than a script so the entry point resolves.
// oxlint-disable-next-line unicorn/require-module-specifiers
export {};
